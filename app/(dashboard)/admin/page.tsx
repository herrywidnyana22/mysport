import { getSession } from "next-auth/react";
import { redirect } from "next/navigation";

const AdminPage = () =>{

    return(
        <div>
            ADMIN PAGE
        </div>
    )
}

export default AdminPage