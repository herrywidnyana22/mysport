export function isBigger(mainValue: number, secondValue: number){
    if(!isNaN(mainValue) && !isNaN(secondValue)){
        if(mainValue > secondValue){
            return true
        } else{
            return false
        }
    } 

    return
                                        
}